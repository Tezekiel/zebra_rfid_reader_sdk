name: zebra_rfid_reader_sdk_example
description: Demonstrates how to use the zebra_rfid_reader_sdk plugin.

publish_to: 'none' 

environment:
  sdk: '>=3.6.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  zebra_rfid_reader_sdk:
    path: ../
  cupertino_icons: ^1.0.8
  permission_handler: ^12.0.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
